// Root-level config for Seqera Platform — delegates to the real config.
// Nextflow only auto-loads nextflow.config from the project root, so this
// includeConfig is required when "Main script" is nextflow/gwas/main.nf.
includeConfig 'nextflow/gwas/nextflow.config'
